<!-- index.hbs -->
<h1>The server is running on Event!</h1>
<p>If you see this text, the server is <strong>definitely</strong> working!</p>
<!-- <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script> -->

<table id="event-table">
    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
            <th>delete</th>
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr data-value={{this.id}}>
            <td>{{this.event_id}}</td>
            <td>{{this.event_attendance}}</td>
            <td>{{this.event_type}}</td>
            <td>{{this.date}}</td>
            <td>{{this.exhibit_name}}</td>
            <td>{{this.host_name}}</td>
            <td><button onclick="deleteEvent({{this.event_id}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
<script src="./js/delete_event.js"></script>

<!-- /views/index.hbs -->
<h2>Adding Data using an HTML form</h2>
<p>To add a new event, please enter its information below and click `Submit`!</p>
<form id="add-event-form" method="POST" action="/add-event-form">
    <label for="input-event-type">Event Type: </label>
    <input type="text" name="input-event-type" id="input-event-type">
    
    <label for="input-event-date">Event Date: </label>
    <input type="date" name="input-date" id="date">

    <label for="input-exhibit-name">Exhibit Name: </label>
    <input type="text" name="input-exhibit-name" id="input-exhibit-name">

    <label for="input-host-name">Host Name: </label>
    <input type="text" name="input-host_name" id="input-host_name">

    <input type="submit">
</form>
<script src="./js/add_event.js"></script>
<!--
<label for="input-fullname">Full name: </label>
    <select name="input-fullname" id="mySelect">
        <option value="test">Select a Person</option>
        {{#each data}}
        <option value="{{this.id}}">{{this.first_name}} {{this.last_name}}</option>
        {{/each}}
    </select>
-->

<h2>Search Using an HTML Form</h2>
<p>Search by last name using the field below. Partial last names are OK too!</p>
<form id="search-employee-html" method='GET' action='/employee'>
    <label for="input-last_name">Search by Last Name: </label>
    <input type="text" name="last_name" id="input-last_name-html-search">
    
    <input type="submit">
    <a href='/employee'><button>Reset</button></a>
</form>

<h2>Updating An Employee's Job Form</h2>
<p>To update a specific employee's job select their name from the drop-down menu on the left and their desired job from the drop down menu on the right</p>
<form id="update-employee-form-ajax">

    <label for="input-fullname">Full name: </label>
    <select name="input-fullname" id="mySelect">
        <option value="test">Select an Employee</option>
        {{#each data}}
        <option value="{{this.employee_id}}">{{this.first_name}} {{this.last_name}}</option>
        {{/each}}
    </select>

    <label for="input-job">Job: </label>
    <select name="input-job" id="input-job-update">
        <option>Select a Job</option>
        {{#each jobs}}
        <option value="{{this.employee_id}}">{{this.job}}</option>
        {{/each}}
    </select>
    <input type="submit">

</form>

<script src="./js/update_employee.js"></script>